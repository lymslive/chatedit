# 带系统提示的对话示例

## system >> 请使用中文回答，保持简洁。

## user >> 什么是 REST API？
请用一段话解释。

## assistant >> REST API 是一种基于 HTTP 协议的接口规范，
使用标准的 GET/POST/PUT/DELETE 方法对资源进行操作，
以 JSON 或 XML 格式交换数据，具有无状态、统一接口等特点。

## user >> 举一个具体的例子

# 注释：以下是用户追问，实际发送时只包含上方的对话内容
